using UnityEngine;

public class HostGameManager : MonoBehaviour
{
    // The Host is also a client, so authentication happens via the client.
    // Init logic will be added here for server setup in later steps.
}
